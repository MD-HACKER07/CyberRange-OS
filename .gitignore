# Env / secrets
.env
.env.local
.env.deploy
*.env
# keep the templates
!.env.example
!.env.deploy.example

# Go
/apps/api/api
/apps/api/seed
*.exe
build_errors.txt
vet_errors.txt

# Node
node_modules/
.next/
out/

# OS
.DS_Store
Thumbs.db

# Local tooling / scratch
git_push.ps1
gp.ps1
check.ps1
*_out.txt
*_result.txt
apps/api/h.json
apps/web/tsconfig.tsbuildinfo

# LaTeX aux
docs/*.aux
docs/*.out
docs/*.toc
docs/*.log
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz

# Training artifacts
training/dataset.jsonl
training/dataset_eval.jsonl
training/enterprise-attack.json
training/*.gguf
training/outputs/

# Data
/data/
storage/

# Scratch / scratch deploy artifacts (never commit)
_*
deploy/_*
deploy/push.tar.gz
*.tar.gz
LOGIN_CREDENTIALS.txt
*.pem
*.key

# Model weights (distributed via GitHub Releases, never committed to the repo)
*.gguf

# Private institutional / student data — MUST NEVER be published
sanjivani-b-tech-cyber-security-*.json
sanjivani_timetables.json
*student*.json

# Release build artifacts (uploaded to GitHub Releases, not committed)
deploy/*.zip
*.zip
